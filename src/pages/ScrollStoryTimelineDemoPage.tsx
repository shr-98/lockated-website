import { ScrollStoryTimeline, DEFAULT_SCROLL_STORY_STEPS } from '../components/ScrollStoryTimeline'

/**
 * Dev / demo route for the scroll story component (replace `steps` with your content).
 */
export default function ScrollStoryTimelineDemoPage() {
  return <ScrollStoryTimeline steps={DEFAULT_SCROLL_STORY_STEPS} />
}
